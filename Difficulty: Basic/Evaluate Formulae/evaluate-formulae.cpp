#include <iostream>
using namespace std;

int main() {
    int a, b, c, d;
    cin >> a >> b >> c >> d;
    
    // Single statement expression evaluating the formula
    cout << (a + b) / c + d;
    
    return 0;
}