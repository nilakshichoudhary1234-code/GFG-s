void utility(int number) {
    // Check if the number is greater than 100
    if (number > 100) {
        cout << "Big" << endl;
    } 
    // Otherwise, it must be smaller than or equal to 100
    else {
        cout << "Number" << endl;
    }
}